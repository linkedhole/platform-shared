// 웹어플리케이션 util, handler... 공용 모듈
description = 'Platform Shared Support (shared)'

defaultTasks 'clean', 'build', 'uploadArchives'

uploadArchives {
    repositories.mavenDeployer {
        pom.version = "1.0"
        pom.artifactId = 'shared-support'
    }
}
