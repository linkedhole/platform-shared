// 웹어플리케이션 비지니스 공용 모듈
description = 'Platform Shared Application (shared)'

defaultTasks 'clean', 'build', 'uploadArchives'

uploadArchives {
    repositories.mavenDeployer {
        pom.version = "1.0"
        pom.artifactId = 'shared-application'
    }
}
