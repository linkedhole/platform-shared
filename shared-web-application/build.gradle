// 웹어플리케이션 웹 공용 모듈
description = 'Platform Shared Web Application (shared)'

defaultTasks 'clean', 'build', 'uploadArchives'

uploadArchives {
    repositories.mavenDeployer {
        pom.version = "1.0"
        pom.artifactId = 'shared-web-application'
    }
}
